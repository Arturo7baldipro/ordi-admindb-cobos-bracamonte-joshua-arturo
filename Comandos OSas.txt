
Abrir sql en el cotenedor:
docker exec -it mysql_database mysql -u root -prootpassword

Abrir el bash:
docker exec -it mysql_database bash

ejecutar (recuerda por cada cambio hacer el primer comando):
docker cp .\scripts\transaccion.sql mysql_database:/tmp/transaccion.sql
mysql -u root -prootpassword < /tmp/transaccion.sql

conexion a dbeaver:
allowPublicKeyRetrieval = true
useSSL = false
serverTimezone = UTC

Host: localhost
Puerto: 3306
Usuario: root
Contraseña: rootpassword
Base de datos: a eleccion
